# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.con##############,auto # HDMI monitor is upper then laptop monitor

monitor = eDP-1, preferred , auto, 1.0
monitor = HDMI-A-1, preferred, auto-right, 1.0

workspace = 1,monitor:eDP-1, default:true
workspace = 2,monitor:eDP-1, default:true
workspace = 3,monitor:eDP-1, default:true
workspace = 4,monitor:eDP-1, default:true
workspace = 5,monitor:eDP-1, default:true
workspace = 6,monitor:eDP-1, default:true
workspace = 7,monitor:eDP-1, default:true

workspace = 8,monitor:HDMI-A-1, default:true
workspace = 9,monitor:HDMI-A-1, default:true
workspace = 10,monitor:HDMI-A-1, default:true
workspace = 11,monitor:HDMI-A-1, default:true
workspace = 12,monitor:HDMI-A-1, default:true
workspace = 13,monitor:HDMI-A-1, default:true
workspace = 14,monitor:HDMI-A-1, default:true
workspace = 15,monitor:HDMI-A-1, default:true


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nautilus
$menu = rofi --show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar
exec-once = hyprpaper # & firefox
# exec-once = su $SUDO_USER:$SUDO_USER -E nekoray
# exec-once = /home/fueiret/.config/scripts/battery-alert
exec-once = env DISPLAY=$DISPLAY WAYLAND_DISPLAY=$WAYLAND_DISPLAY XDG_RUNTIME_DIR=$XDG_RUNTIME_DIR nekoray



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 5,10,10,10

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(63d5f7aa) 
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 2

        ignore_opacity = true
        new_optimizations = true

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1


    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    # pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    col.splash = 0xffffffff
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,ru
    kb_variant =
    kb_model = # pc105
    kb_options = grp:alt_shift_toggle
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    gesture = 3, horizontal, workspace
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Volume controls
# bind = , XF86AudioRaiseVolume, exec, sh ~/.config/scripts/volume --up
# bind = , XF86AudioLowerVolume, exec, sh ~/.config/scripts/volume --down
# bind = , XF86AudioMute, exec, sh ~/.config/scripts/volume --toggle

# Brightness controls
# bind = , XF86MonBrightnessUp, exec, sh ~/.config/scripts/brightness.sh --up
# bind = , XF86MonBrightnessDown, exec, sh ~/.config/scripts/brightness.sh --down

# Screenshot
#bind = , PRINT, exec, flameshot gui

# Change neighboor workspaces
# bind = SUPER, G, exec, hyprctl dispatch swapactiveworkspaces current +1



# Screenshot a window (TODO: Fix it)
# bind = $mainMod, PRINT, exec, hyprshot -m window
# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output --clipboard-only
# Screenshot a region
bind = $mainMod SHIFT, S, exec, hyprshot -m region --clipboard-only

# Screenshot a window (TODO: Fix it)
# bind = $mainMod, PRINT, exec, hyprshot -m window
# Screenshot a monitor
bind = $mainMod SHIFT, PRINT, exec, hyprshot -m output
# Screenshot a region
bind = SHIFT, PRINT, exec, hyprshot -m region
  
# Hyprland reload
bind = $mainMod ESCAPE, R, exec, killall -USR1 hyprland && notify-send 'Hyprland' 'Reloaded config' -t 1500

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = ALT, F4, killactive,
bind = $mainMod SHIFT, Q, exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod ALT, L, exec, hyprlock
# bind = $mainMod, R, exec, $menu
bind = $mainMod SHIFT, C, exec, codium # codium
bind = $mainMod SHIFT, F, exec, librewolf# firefox
bind = $mainMod SHIFT, Y, exec, yandex-music # firefox
bind = $mainMod SHIFT, T, exec, Telegram# telegram
bind = $mainMod, Print, exec, grim -g "$(slurp)" - | wl-copy

# change language (should fix notifications)
bind = ALT, SHIFT, exec, sh ~/.config/scripts/change-language.sh
bind = SHIFT, ALT, exec, sh ~/.config/scripts/change-language.sh


# Rofi menu
bind = SUPER, D, exec, rofi -show drun
bind = SUPER, TAB, exec, rofi -show window
bind = SUPER, X, exec, sh ~/.config/scripts/powermenu.sh

# Wallpaper and themes
bind = $mainMod, W, exec, bash ~/.config/scripts/wallpapers-selector.sh
bind = $mainMod, P, exec, sh ~/.config/scripts/waybar-theme-selector.sh
bind = $mainMod, R, exec, sh ~/.config/scripts/rofi-theme-selector.sh


##############################
### WINDOWS AND WORKSPACES ###
##############################

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod CTRL, 1, workspace, 11
bind = $mainMod CTRL, 2, workspace, 12
bind = $mainMod CTRL, 3, workspace, 13
bind = $mainMod CTRL, 4, workspace, 14
bind = $mainMod CTRL, 5, workspace, 15

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = $mainMod SHIFT CTRL, 1, movetoworkspace, 11
bind = $mainMod SHIFT CTRL, 2, movetoworkspace, 12
bind = $mainMod SHIFT CTRL, 3, movetoworkspace, 13
bind = $mainMod SHIFT CTRL, 4, movetoworkspace, 14
bind = $mainMod SHIFT CTRL, 5, movetoworkspace, 15


# Move focus
bind = $mainMod, H, movefocus, l
bind = $mainMod, J, movefocus, d
bind = $mainMod, K, movefocus, u
bind = $mainMod, L, movefocus, r

# Move window
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, J, movewindow, d
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, L, movewindow, r

# Resize windows
bind = $mainMod CTRL, H, resizeactive, -20 0
bind = $mainMod CTRL, J, resizeactive, 0 20
bind = $mainMod CTRL, K, resizeactive, 0 -20
bind = $mainMod CTRL, L, resizeactive, 20 0

bind = $mainMod SHIFT CTRL, H, resizeactive, 20 0
bind = $mainMod SHIFT CTRL, J, resizeactive, 0 -20
bind = $mainMod SHIFT CTRL, K, resizeactive, 0 20
bind = $mainMod SHIFT CTRL, L, resizeactive, -20 0

# Window style managment
bind = $mainMod, SPACE, togglefloating
bind = $mainMod, F, fullscreen
bind = , F11, fullscreen
bind = $mainMod, T, pseudo, # dwindle
# bind = $mainMod, S, togglesplit, # dwindle
bind = ALT, TAB, cyclenext
bind = ALT SHIFT, TAB, cyclenext, prev

bind = SUPER, G, togglegroup
# bind = SUPER, CTRL, tab, 

# Float window movement
# bind = CTRL ALT, A, movewindow, l
# bind = CTRL ALT, D, movewindow, r
# bind = CTRL ALT, W, movewindow, u
# bind = CTRL ALT, S, movewindow, d

# Example special workspace (scratchpad)
bind = CTRL, TAB, togglespecialworkspace, magic
bind = $mainMod SHIFT, TAB, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, sh ~/.config/scripts/volume.sh --up # pactl set-sink-volume @DEFAULT_SINK@ +5%
bindel = ,XF86AudioLowerVolume, exec, sh ~/.config/scripts/volume.sh --down # pactl set-sink-volume @DEFAULT_SINK@ -5%
bindel = ,XF86AudioMute, exec, sh ~/.config/scripts/volume.sh --toggle # pactl set-sink-mute @DEFAULT_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, sh ~/.config/scripts/brightness.sh --up # brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, sh ~/.config/scripts/brightness.sh --down # brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# layerrules
# waybar
# windowrulev2 = suppressevent maximize, class:^(waybar)$
# layerrule = blur, waybar

# rofi
windowrule {
    name = rofi-opacity-rounding
    match:class = ^(rofi)$
    opacity = 0.9 override 0.9 override
    rounding = 10
}

# Ignore maximize requests from apps. You'll probably like this.
windowrule {
    name = ignore-maximize
    match:class = .*
    suppress_event = maximize
}

# Fix some dragging issues with XWayland
windowrule {
    name = xwayland-drag-fix
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false
    no_focus = on
}

# nautilus
windowrule {
    name = nautilus-float
    match:class = ^org\.gnome\.Nautilus$
    float = on
    size = 50% 50%
    center = on
}

# Pavucontrol
windowrule {
    name = pavucontrol-float-pin
    match:class = org.pulseaudio.pavucontrol
    float = on
    size = (monitor_w*0.34) (monitor_h*0.3)
    move = (monitor_w*0.65) (monitor_h*0.05)
    # center = on
    pin = on
    # stay_focused = on
}

# blueman-manager
windowrule {
    name = blueman-float-pin
    match:class = ^blueman-manager$
    float = on
    size = (monitor_w*0.34) (monitor_h*0.3)
    move = (monitor_w*0.65) (monitor_h*0.05)
    # center = on
    pin = on
    # stay_focused = on
}

# nmtui
windowrule {
    name = nmtui-float-center
    match:class = ^Nmtui$
    float = on
    center = on
}

# Picture in picture
windowrule {
    name = pip-float-pin
    match:title = ^Picture-in-Picture$
    float = on
    pin = on
    center = on
    stay_focused = on
}

# Nekoray
windowrule {
    name = nekoray-workspace
    match:class = nekobox
    workspace = 7 silent
}

# dialog window
windowrule {
    name = dialogs-float
    match:title = (.*[Dd]ialog.*)
    float = on
    center = on
    stay_focused = on
}
